<div class="container">
  <div class="row mt-5 mb-1">
    <div class="col-12">
    <h1><%= @plane.name %></h1>
    </div>
  </div>
  <div class="row mb-3">
    <div class="col-12">
    <div class="d-flex align-items-end justify-content-end">
    <% if policy(@plane).edit? %>
      <%= link_to 'Edit', edit_plane_path(@plane), class: "simple-link mr-3" %>
      <% if @plane.availability %>
        <%= simple_form_for @plane do |f| %>
          <%= f.input :availability, as: :hidden, input_html: { value: false } %>
          <%= f.submit "Delete", class: 'simple-link-delete' %>
        <% end %>
      <% end %>
    <% end %>
    </div>
    </div>
  </div>
  <div class="row mb-5">
    <div class="col-12">
      <% if @plane.photo.attached? %>
        <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('<%= cl_image_path @plane.photo.key %>');"></div>
      <% else %>
        <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/greece.jpg');"></div>
      <% end %>
    </div>
  </div>
  <div class="row">
    <div class="col-12 col-lg-7 mb-5">
      <div class="plane-card">
        <h3><%= @plane.plane_model.name %></h3>
          <div class="light-border-bottom">
            <ul class="list-inline mb-0">
              <li class="list-inline-item">
                <i class="fas fa-chair"></i> <%= @plane.plane_model.seats %>
              </li>
              <li class="list-inline-item">
                <i class="fas fa-horse"></i> <%= @plane.plane_model.horsepower %>
              </li>
            </ul>
          </div>
          <p> rented by: <%= @plane.user.email %></p>
          <p class="mt-3 mb-0">Lorem ipsum dolor sit amet, consetetur sadipscing elitr, sed diam nonumy eirmod tempor invidunt ut labore et dolore magna aliquyam erat, sed diam voluptua. At vero eos et accusam et justo duo dolores et ea rebum. Stet clita kasd gubergren, no sea takimata sanctus est Lorem ipsum dolor sit amet. Lorem ipsum dolor sit amet, consetetur sadipscing elitr, sed diam nonumy eirmod tempor invidunt ut labore et dolore magna aliquyam erat, sed diam voluptua. At vero eos et accusam et justo duo dolores et ea rebum. Stet clita kasd gubergren, no sea takimata sanctus est Lorem ipsum dolor sit amet.</p>
      </div>
    </div>
  <div class="col-12 col-lg-5">
    <div class="plane-card">
      <p><span style="font-size: 24px;"><strong><%= @plane.price.to_f / 100 %> €</strong></span> / day<p>
      <%= simple_form_for [ @plane, @booking ] do |f| %>
        <%= f.input :status, as: :hidden, input_html: { value: "pending" } %>
        <%= f.input :start_date, as: :date, html5: true %>
        <%= f.input :end_date, as: :date, html5: true %>
        <%= f.submit "Book now", class: "cta cta-dark" %>
      <% end %>
    </div>
  </div>
</div>
